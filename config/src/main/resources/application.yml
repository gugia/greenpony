server:
  port: 14000
spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gugia/greenpony.git
          username:
          password:
          search-paths: '{application}'
      label: master
